@model IEnumerable<In_Mgmt.Models.StoredUOM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            SUOM_Name
        </th>
        <th>
            SUOM_Code
        </th>
        <th>
            Factor
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SUOM_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SUOM_Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Factor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SUOMID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SUOMID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SUOMID })
        </td>
    </tr>
}

</table>

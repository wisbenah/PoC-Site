@model IEnumerable<In_Mgmt.Models.InventoryUOM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            IUOM_Name
        </th>
        <th>
            SUOM_Code
        </th>
        <th>
            Factor
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IUOM_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SUOM_Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Factor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IUOMID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IUOMID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IUOMID })
        </td>
    </tr>
}

</table>

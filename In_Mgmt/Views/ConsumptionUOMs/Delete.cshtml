@model In_Mgmt.Models.ConsumptionUOM

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ConsumptionUOM</legend>

    <div class="display-label">CUOM_Name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CUOM_Name)
    </div>

    <div class="display-label">CUOM_Code</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CUOM_Code)
    </div>

    <div class="display-label">Factor</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Factor)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
